#include <stdio.h>

int check_prop_() {
}


// recompute all expressions used in LTL syntax
ltl_var_1 = x;
ltl_var_2 = y;

void update_ltl_vars() {

}

int main(int argc, char const *argv[])
{
    while (1) {
        a = get_action(s, env);
        s, env = update(s, env, a);
        update_ltl_vars();
    }
    return 0;
}
